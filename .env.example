# Copy this to .env for local development only.
# In production, set these in Portainer's stack environment variables.

PORT=3000
DB_PATH=/data/homesked.db
JWT_SECRET=replace-with-a-long-random-string

# For future mobile app support — set to your server's LAN/public IP
# e.g. CORS_ORIGINS=http://192.168.1.100:3000,https://yourdomain.com
CORS_ORIGINS=*
